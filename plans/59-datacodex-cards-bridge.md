
# خطة 59 — جسر كروت الحالات من Datacodex

> **الإصدار:** 3.0.0 · **التاريخ:** 2026-09-13
> **الحالة:** مسودة — بانتظار اعتماد المطور
> **المصدر:** مواصفات مسلَّمة من مشروع Datacodex Content Tool

---

## 1. الهدف

صفحات الخدمة في الفارس **خالية حالياً من أي صور ومن أي محتوى متجدد**. هذه الخطة تجعلها تعرض كروتاً لمحتوى موثّق منشور على `datacodexlab.com`، وتتحدث تلقائياً مع كل نشر جديد هناك — **بصفر تدخل يدوي في هذا المشروع بعد التنفيذ**.

## 2. السياق الاستراتيجي — اقرأه قبل أي كود

العلاقة موثّقة رسمياً في `Datacodex/docs/SEO_ARCHITECTURE.md` بنموذج **Hub & Spoke**:

| الطرف | الدور |
|---|---|
| **datacodexlab.com** | المركز — سلطة معرفية رقمية، هوية محمولة غير مقيدة جغرافياً |
| **alfareslab.com** | الطرف — الكيان الجغرافي الفعلي في جدة ونقطة استلام العملاء |

الربط **منفَّذ فعلاً** في `seo/structured-data.json` بهذا المشروع: الموظف معرَّف بـ`https://datacodexlab.com/#ahmed-saleh`، وحسابات التواصل مشتركة، والتذييل يصرّح بالعلاقة.

**اتجاه تدفق السلطة مقصود: من الفارس → داتا كودكس.**

**العدو المُسمّى: تضارب الكلمات المفتاحية (Cannibalization).** لذلك:

> 🚫 **لا يُنسخ نص أي مقال من داتا كودكس إلى هذا الموقع إطلاقاً.**
> الكارت يحمل عنواناً وسطر ملخص وصورة ورابطاً — لا أكثر.

## 3. اكتشاف حاكم عن هذا المشروع

**هذا المشروع لا يملك خطوة بناء إطلاقاً.** لا `package.json`، لا GitHub Actions، لا أي إعداد بناء. ما يجري نشر تلقائي لا بناء.

**قرار المطور:** تُضاف خطوة بناء بسيطة. الموقع يبقى ستاتيك وشكله لا يتغير.

**مرفوض صراحةً:**
- ❌ تحميل الكروت بجافاسكريبت بعد فتح الصفحة — لن تراها محركات البحث، ويلغي الهدف كله
- ❌ التحديث اليدوي — يخالف «صفر تدخل يدوي»
- ❌ **بناء إلى `dist/` بقائمة ملفات مسموحة** — مرفوض بقرار موثَّق: أي ملف جديد لا يُضاف للقائمة يختفي من الإنتاج بصمت. البديل: **تعديل في المكان بأربعة ضمانات** (القسم 6)

---

## 4. مصدر البيانات

موقع داتا كودكس يولّد وقت بنائه ملف `feed.json` — **نقطة التماس الوحيدة**، على العنوان
الثابت:

```
https://datacodexlab.com/feed.json
```

### عقد القائمة — تحقق منه ولا تفترض صحته

| البند | القاعدة |
|---|---|
| `schemaVersion` | **يجب أن يساوي `2` تماماً (رقم صحيح)** — أي قيمة أخرى، غائبة أو نصية أو مختلفة، تُفشل البناء |
| شكل الجذر | كائن يحمل `schemaVersion` و`items[]` |
| `id` | نص فريد ومستقر لكل عنصر — **لا يُستخدم للترتيب ولا للعرض**، فقط لإزالة التكرار دفاعياً (انظر سياسة الفشل، القسم 5) |
| `title` | نص |
| `cardSummary` | سطر مكتوب خصيصاً للكارت — **ليس منقولاً من المقال** |
| `image` | مطلق · HTTPS · نطاق `datacodexlab.com` |
| `url` | مطلق · HTTPS · نفس النطاق · **بشرطة مائلة ختامية** |
| `topics[]` | مصفوفة نصوص غير فارغة، **بحد أقصى عنصرين**، تصف **موضوع المحتوى** — لا تحمل أي معرّف صفحة ولا أي علم بوجود موقع الفارس |
| `lang` | `ar` \| `en` |
| `type` | `post` \| `case` \| `service` |
| `publishedAt` | ISO 8601 بـUTC |
| `hasVideo` | منطقي |

**مثال حقيقي — عنصر واحد كما هو منسوخ حرفياً من الفيد الحي بتاريخ 2026-09-13** (توضيحي
لشكل العنصر فقط، **ليس قائمة معتمدة ولا مرجعاً دائماً** — القيم الفعلية تتغير مع كل نشر
جديد على داتا كودكس):

```json
{
  "id": "post:en:klevv-neo-n400-ssd-recovery",
  "title": "KLEVV NEO N400 SSD Data Recovery: Resolving SM2258XT Firmware Panic Protocols",
  "cardSummary": "KLEVV NEO N400 SSD Data Recovery | SM2258XT Firmware Fix",
  "image": "https://datacodexlab.com/_astro/klevv-neo-n400-ssd-recovery.B3OCTYRi_q35KR.webp",
  "url": "https://datacodexlab.com/en/posts/en-klevv-neo-n400-ssd-recovery/",
  "topics": ["hdd-internal", "ssd-nvme"],
  "lang": "en",
  "type": "post",
  "publishedAt": "2026-08-30T00:00:00.000Z",
  "hasVideo": false
}
```

🛑 **يُفشل البناء كله مع تسجيل السبب:** عنصر يخالف **شكل** العقد (مثال: `topics[]` ليست
مصفوفة نصوص، أو فارغة، أو تحمل أكثر من عنصرين) · رابط خارج النطاق أو غير HTTPS · `url`
مكرر · عنصر ناقص حقلاً مطلوباً · `schemaVersion` لا يساوي `2`.

> ⚠️ **لا تستبعد هذه العناصر وتكمل البناء.** الاستبعاد الصامت ينشر موقعاً ناقصاً بلا إنذار.
> **الاستثناءان الموثقان الوحيدان من هذه القاعدة: تكرار `id`، ومعرّف موضوع صالح الشكل
> لكنه غير موجود في جدول هذا القسم** — كلاهما موثّق بدقة في سياسة الفشل، القسم 5، ولا
> يُسقَط أي منهما صامتاً، بل بتسجيل صريح في سجل البناء. لا يُوسَّع أي منهما لأي حالة
> أخرى.
>
> **مهم:** معرّف موضوع صالح الشكل (نص، ضمن حد العنصرين) لكنه غير موجود في جدول
> "خريطة المواضيع إلى صفحات الفارس" أدناه **ليس مخالفة عقد ولا يُفشل البناء إطلاقاً.**
> هذا **متوقع تماماً** إن أضافت داتا كودكس مستقبلاً قيمة جديدة لقائمتها المغلقة قبل أن
> يعرف بها هذا الجدول — الفيد يبقى صالحاً بعقده، والعلاج مراجعة الجدول أدناه لا إصلاح
> شيء في السكربت. السلوك المطلوب: استبعاد طبيعي للعنصر من أي صفحة، مع **تسجيل تحذير
> صريح في سجل البناء يذكر المعرّف غير المعروف بالاسم** — تفصيل كامل في القسم 5.

### خريطة المواضيع إلى صفحات الفارس

**هذا الجدول ملك لمشروع الفارس وحده. الفيد لا يعرف بوجوده ولا يلتزم بمزامنته.**
الفيد يرسل `topics[]` — موضوع محتوى بحت، لا معرّف صفحة. الربط بين موضوع وصفحة خدمة فعلية
في الفارس **قرار يخص هذا المشروع حصراً**، يُصان بالكامل هنا داخل السكربت (القسم 7،
المجموعة 2). لو تغيّرت القائمة المغلقة للمواضيع مستقبلاً — بالإضافة أو الحذف — فلن يصل
أي إخطار تلقائي؛ مراجعة هذا الجدول عند كل تحديث لهذا الملف مسؤولية وكيل الفارس.

**المعرّف الإنجليزي هو ما يُطابَق عليه.**

| المعرّف | الصفحة العربية | الصفحة الإنجليزية |
|---|---|---|
| `hdd-internal` | `services/hdd-data-recovery.html` | `en/services/hdd-data-recovery.html` |
| `hdd-external` | `services/external-hdd-data-recovery.html` | `en/services/external-hdd-data-recovery.html` |
| `ssd-nvme` | `services/ssd-nvme-data-recovery.html` | `en/services/ssd-nvme-data-recovery.html` |
| `laptop-pc` | `services/laptop-pc-data-recovery.html` | `en/services/laptop-pc-data-recovery.html` |
| `mac` | `services/mac-data-recovery.html` | `en/services/mac-data-recovery.html` |
| `flash-sd` | `services/flash-sd-data-recovery.html` | `en/services/flash-sd-data-recovery.html` |
| `raid-nas` | `services/raid-nas-data-recovery.html` | `en/services/raid-nas-data-recovery.html` |
| `dvr-nvr` | `services/dvr-nvr-data-recovery.html` | `en/services/dvr-nvr-data-recovery.html` |
| `ransomware` | `services/ransomware-data-recovery.html` | `en/services/ransomware-data-recovery.html` |
| `database-erp` | `services/database-erp-recovery.html` | `en/services/database-erp-recovery.html` |

**قاعدة المطابقة: احتواء مصفوفة، لا مساواة.** عنصر يُعرَض على صفحة خدمة إذا كان
`topics[]` **يحتوي** معرّف تلك الصفحة (`topics.includes(pageTopicId)`) — بصرف النظر
عن أي قيمة أخرى موجودة معه في نفس المصفوفة.

**عنصر واحد يحمل موضوعين يظهر على صفحتَي خدمة مختلفتين في آنٍ واحد — هذا سلوك متوقع
وطبيعي، وهو بالضبط المقصود من سقف العنصرين في العقد، وليس خطأ بيانات ولا تكراراً
يجب إصلاحه.** الفيد الحي يستخدم هذه الحالة فعلياً اليوم: عنصر بـ`topics: ["hdd-internal",
"ssd-nvme"]` يظهر على صفحة الهارد الداخلي وصفحة SSD/NVMe معاً.

**صفحتا المناطق** (`data-recovery-makkah` · `data-recovery-saudi-arabia`): لا تُطابق على معرّف — تعرضان **أحدث المحتوى الموجود في القائمة** بصرف النظر عن الصفحة.

**`about-lab` و`privacy-policy` مستبعدتان تماماً.**

### الترتيب

> **عضوية الصفحة هي الصلة** (فلترة نعم/لا)، **ثم الترتيب بالحداثة**.

القائمة لا تحمل درجة صلة — **لا تخترع ترتيباً**. في الرئيسية: الحداثة أولاً.

---

## 5. سياسة الفشل — اقرأها بعناية

هذا أهم قرار تشغيلي في الخطة.

| الحالة | السلوك |
|---|---|
| قائمة صالحة بلا عناصر مطابقة لهذه الصفحة | ✅ **بناء ناجح بلا كروت** |
| عنصر **مطابق للعقد** لكنه لا يطابق لغة الصفحة أو لا يحتوي `topics[]` على معرّف هذه الصفحة | ✅ **استبعاد طبيعي من تلك الصفحة فقط** — البناء يكمل |
| عنصر يحمل معرّف موضوع **صالح الشكل** (نص ضمن حد العنصرين) لكنه **غير موجود في جدول "خريطة المواضيع إلى صفحات الفارس"** (القسم 4) | ✅ **استبعاد طبيعي من كل الصفحات، بلا إفشال بناء** — **يُسجَّل صراحة في سجل البناء بالمعرّف غير المعروف باسمه**. هذا **متوقع** عند توسّع القائمة المغلقة لدى داتا كودكس قبل تحديث هذا الجدول — العلاج مراجعة الجدول لا إصلاح السكربت. هذه ليست مخالفة عقد؛ العنصر صالح تماماً بعقده الأصلي |
| عنصران يحملان نفس `id` | ✅ **إسقاط دفاعي للنسخة المكررة فقط، بلا إفشال بناء** — **يُسجَّل صراحة في سجل البناء بالمعرّف المكرر** وسبب الإسقاط، وتُبقى النسخة الأولى المصادَفة. هذا استثناء موثَّق من قاعدة "لا إسقاط صامت" أدناه: الممنوع هو **الصمت**، لا الإسقاط — التسجيل الصريح شرط ملزم لا اختياري، ولا يُوسَّع هذا الاستثناء لأي حالة أخرى |
| **فشل تنزيل أو التحقق من صورة عنصر صالح** | 🛑 **أفشل البناء** |
| فشل شبكة · HTTP غير ناجح · JSON تالف · مخالفة العقد · `schemaVersion` لا يساوي `2` | 🛑 **أفشل البناء** |

**السبب:** لو بنيت بنجاح بلا كروت عند فشل الشبكة، فأي انقطاع مؤقت **يمحو كل الكروت من الموقع الحي وينشر ذلك** — ومحدش يلاحظ لأن البناء «نجح». إفشال البناء يُبقي آخر Deployment سليماً.

> **القاعدة الفاصلة:** الاستبعاد الطبيعي مسموح **فقط** بعد نجاح تحقق العقد، ولثلاثة أسباب حصراً: عدم مطابقة اللغة، عدم مطابقة الصفحة، أو معرّف موضوع صالح الشكل غير معروف محلياً (مسجَّل صراحة). تكرار `id` معالَج صراحة أعلاه كاستثناء موثَّق مسجَّل صراحة في سجل البناء. كل ما عدا هذه الحالات — فشل بناء، **لا إسقاط صامت لأي عنصر آخر**.

- [ ] [🤖] timeout صريح للطلب
- [ ] [🤖] رسالة خطأ واضحة بلا أسرار
- [ ] [🤖] **Flag انتقالي مؤقت** يسمح بالبناء بلا قائمة **قبل الإطلاق الأول فقط** — يُزال بعده

---

## 6. الضمانات الأربعة للتعديل في المكان

| # | الضمان |
|---|--------|
| 1 | **حقن Idempotent** — استبدال المنطقة بين علامة بداية وعلامة نهاية، لا الإضافة إليها |
| 2 | **مسارات ملفات صريحة مُعدَّدة في السكربت — ممنوع أي glob** |
| 3 | **تشغيلان متتاليان ينتجان ملفات متطابقة Byte-for-byte** — اختبار قبول إلزامي |
| 4 | **لا Commit لأي HTML مولَّد** — نسخة Cloudflare مؤقتة |

> ⚠️ الضمان 2 ليس شكلياً: المستودع يحتوي عدداً كبيراً من ملفات `sync-conflict` غير المتتبعة. أي glob سيلتقطها.

---

## 7. خطوات التنفيذ

### المجموعة 1: التحضير والحماية

- [ ] [🤖] **ابدأ من نسخة نظيفة** عند آخر commit معتمد — المستودع فيه ~99 ملف غير نظيف
- [ ] [🤖] **احسم فرع النشر صراحةً** — المحلي على `master` بينما `origin/HEAD` يشير إلى `main`. لا تعمل على فرع لا يُنشر
- [ ] [👤] **المطور:** تسجيل Snapshot لإعداد Cloudflare — Production branch · Root directory · Build command · Output directory · Node version · متغيرات البيئة · إعدادات Preview/Production
- [ ] [🤖] توثيق **مسار rollback دقيق** خطوة بخطوة

⏸️ **Review Gate 1** — لا شيء يبدأ قبل وجود Snapshot ومسار رجوع.

### المجموعة 2: السكربت

- [ ] [🤖] `scripts/build-cards.mjs` — **بدون أي مكتبة خارجية**، Node المدمج فقط
- [ ] [🤖] جلب القائمة والتحقق من العقد بالكامل قبل استخدامها
- [ ] [🤖] الفلترة باللغة واحتواء `topics[]` على معرّف الصفحة (`topics.includes(pageTopicId)`)، ثم الترتيب بالحداثة
- [ ] [🤖] تطبيق سياسة الفشل (القسم 5) والضمانات الأربعة (القسم 6)
- [ ] [🤖] **تنزيل الصور محلياً** إلى `assets/images/datacodex-cards/` — مستبعدة من Git، تُولّد وقت البناء. تحقق من النوع والحجم الأقصى. 🛑 **فشل التنزيل أو التحقق يُفشل البناء** — **ممنوع إسقاط العنصر ومواصلة النشر** (فشل صامت جزئي يخفي كارتاً من الموقع الحي)، و**ممنوع Hotlink بديلاً**
- [ ] [🤖] **تعقيم كامل:** HTML-escape لكل نص · Attribute-escape داخل السمات · التحقق من بروتوكول ونطاق كل رابط · منع `javascript:` · JSON-LD عبر `JSON.stringify` مع منع `</script>` من إنهاء الكتلة
- [ ] [🤖] **سجل تشغيل:** عدد العناصر المستلمة · المحقونة لكل صفحة · سبب كل استبعاد · معرّف أي عنصر أُسقط بسبب تكرار `id` · **أي معرّف موضوع غير معروف صودف أثناء المطابقة، بالاسم**. بدونه تفشل الحزمة بصمت رغم نجاح البناء
- [ ] [🤖] تشغيل محلي + **اختبار التطابق Byte-for-byte**

### المجموعة 3: علامات الموضع

- [ ] [🤖] **علامتان** في كل صفحة خدمة: واحدة **تحت المقدمة** (بعد فقرتَي التمهيد وقبل أول `<h2>`)، وواحدة **قبل دعوة التواصل**
- [ ] [🤖] 12 صفحة عربية + 12 إنجليزية في `en/services/`
- [ ] [🤖] كل علامة تحمل معرّف صفحتها وحدَّي بداية ونهاية للمنطقة المولَّدة
- [ ] [🤖] **التوزيع المثبَّت: 1 بعد المقدمة + 2 في النهاية** — وحدِّد سلوكاً صريحاً عند توفر 0 أو 1 أو 2 عناصر فقط
- [ ] [🤖] **لا تلمس أي شيء آخر في هذه الصفحات** عدا سطرَي العلامة

> **استثناء معلن لقاعدة «لا تلمس»:** لك أن تضيف ملف CSS جديد وتربطه في `<head>` هذه الصفحات، وأن تعدّل الرئيسية وصفحتَي المناطق طبقاً للمجموعة 5. ما عدا ذلك ممنوع.

⏸️ **Review Gate 2** — اعرض السكربت والناتج المحلي. **لا تلمس إعدادات Cloudflare قبل الموافقة.**

### المجموعة 4: تصميم الكارت

التصميم المعتمد: **«مرجع داخل المتن»** — اختير بعد بحث خارجي ومعاينة ثلاثة بدائل.

> **القاعدة الحاكمة: الكارت يجب أن يبدو مُتعمَّداً ألا يبدو كبطاقة محتوى.**
> لغة بصرية **استشهادية/مرجعية**، لا بانر. السبب: العنصر داخل صفحة تجارية ويقود إلى نطاق آخر — لو ظهر كبانر وقع في المنطقة الذهنية التي يتجاهل فيها المستخدم الإعلانات.

- [ ] [🤖] **Desktop:** أفقي — صورة **34%** / محتوى **66%**
- [ ] [🤖] **بلا ظل ثقيل وبلا خلفية ملوّنة.** `border: 1px` هادئ + **شريط رأسي 3–4px عند `inline-start`** — لا تحذفه، هو ما يعطي إحساس المرجع
- [ ] [🤖] الترتيب: `سطر تمهيدي ثابت → العنوان → الملخص → التاريخ → CTA`
- [ ] [🤖] السطر التمهيدي: **«من توثيق أعمالنا على Datacodex»** / `From our documented work on Datacodex`
- [ ] [🤖] الأحجام: تمهيدي `0.75–0.8rem` · عنوان `1.15–1.3rem` وزن 650–700 · ملخص `0.95–1rem` · تاريخ `0.8–0.85rem`
- [ ] [🤖] Padding `20–24px` · عنوان↔ملخص `8–10px` · ملخص↔تذييل `14–16px`
- [ ] [🤖] **الموبايل (< 640–700px): صف وسائط لا stack** — صورة 32–36% على `inline-start` والنص 64–68%. استثناء عند `< 380px` فقط: الصورة 100% فوق النص
- [ ] [🤖] **Hover:** ممنوع رفع الكارت. المسموح: تغيّر خفيف في `border`/`background` + `underline` للـCTA + `scale(1.015)` للصورة داخل `overflow: hidden` بمدة `150–180ms`
- [ ] [🤖] احترام `prefers-reduced-motion: reduce`
- [ ] [🤖] **خصائص CSS منطقية حصراً** — نفس الـDOM يخدم RTL وLTR
- [ ] [🤖] استخدام متغيرات `assets/css/base.css` — والعمل في الوضعين الفاتح والداكن
- [ ] [🤖] الصورة `aspect-ratio: 16/9` و`object-fit: cover` والمساحة محجوزة مسبقاً

**المرجع البصري الجاهز:** `DataCodex_Content_Tool/docs/research/prototypes/prototype-01-inline-reference.html`

### المجموعة 5: قواعد ملزمة (نتائج بحث خارجي)

- [ ] [🤖] `<a href>` عادي — **بدون `rel="nofollow"`**. إرشادات Google: الرابط التحريري الطبيعي لا يحتاجه، وإضافته تناقض هدف المشروع
- [ ] [🤖] **بدون `target="_blank"`** — W3C وGOV.UK: فتح تبويب دون تحذير يربك المستخدم
- [ ] [🤖] **ممنوع زر تشغيل دائري كبير** في منتصف الصورة. البديل: badge صغيرة في الركن `▶ فيديو على Datacodex`. السبب: الفيديو لا يُشغَّل هنا والزر الكبير وعد كاذب
- [ ] [🤖] الاسم المتاح لقارئ الشاشة يتضمن وجود الفيديو وأن الوجهة Datacodex
- [ ] [🤖] **ممنوع «اقرأ المزيد»** — المعتمد: **«اطّلع على التوثيق الكامل على Datacodex»** / `View the full documentation on Datacodex`
- [ ] [🤖] **ممنوع تدوير الكروت عشوائياً** لتغيير الـHTML — يقع تحت سياسات Google للتلاعب
- [ ] [🤖] **JSON-LD — تحسين ثانوي لا بوابة:** `ItemList → ListItem → CreativeWork` · `publisher` = **Datacodex** · **ممنوع** `author: Alfares` · **ممنوع** `mainEntityOfPage` يشير للفارس. الفارس **يستشهد ولا يدّعي الملكية**. أضف اختبارات تثبت ذلك
- [ ] [🤖] زر «استكشف سجل التوثيق الكامل على Datacodex» — **أضفه فقط إن تأكد وجود صفحة أرشيف مستقرة**؛ وإلا أجّله ولا تنشئ دعوة إلى وجهة غير محددة
- [ ] [🤖] سطر صغير أسفل كل مجموعة: «من مدونتنا التقنية — داتا كودكس لاب»

### المجموعة 6: الرئيسية وصفحات المناطق

- [ ] [🤖] شريط **«أحدث ما وثّقناه»** في `index.html` و`en/index.html` أسفل بطاقات الخدمات السبع
- [ ] [🤖] **آخر 3 حالات نجاح فقط** — فلترة على `type = case`؛ المقالات مستبعدة هنا
- [ ] [🤖] في الرئيسية **الحداثة أولاً**
- [ ] [🤖] صفحتا المناطق: أحدث المحتوى الموجود في القائمة، بلا تعليم يدوي

⏸️ **Review Gate 3**

### المجموعة 7: التشغيل الآلي والتحقق

- [ ] [🤖] **أول نشر على Preview URL** بنفس إعدادات Production قبل تحويل Production
- [ ] [🤖] توثيق أمر البناء ومجلد الإخراج ونسخة Node المطلوبة
- [ ] [🤖] توثيق آلية إعادة البناء — **الإشارة تنطلق بعد نجاح بناء داتا كودكس، لا لحظة الـcommit**. Test لا تُطلق الإشارة إطلاقاً
- [ ] [🤖] **تحقق ما بعد النشر:** AR و EN · الموضع الأول والأخير · `_headers` · الروابط النظيفة · `canonical`/`hreflang` · الصفحات التي كانت تحت المراقبة في GSC
- [ ] [🤖] التأكد أن الكارت في **مصدر الصفحة** لا محمَّلاً بجافاسكريبت

> ⚠️ **ضبط لوحة Cloudflare عمل يدوي يخص المطور** — وثّق الخطوات فقط.

⏸️ **Review Gate 4** — نهائية. E2E حقيقي: نشر على داتا كودكس ← ظهور الكارت في الفارس.

---

## 8. الملفات المتأثرة

| الملف | التغيير |
|---|---|
| `scripts/build-cards.mjs` | جديد |
| ملف CSS للكارت + ربطه في `<head>` | جديد |
| `services/*.html` (12) | تعديل — سطرا علامة فقط |
| `en/services/*.html` (12) | تعديل — سطرا علامة فقط |
| `index.html` · `en/index.html` | تعديل |
| `services/data-recovery-makkah.html` · `data-recovery-saudi-arabia.html` (+EN) | تعديل |
| `.gitignore` | تعديل — استبعاد صور الكروت المولَّدة |
| إعداد Cloudflare | **يدوي — المطور** |

## 9. المخاطر

| الخطر | التخفيف |
|---|---|
| **فشل مؤقت يمحو الكروت من الموقع الحي** | البناء يفشل ويبقى آخر Deployment سليم |
| **تكرار الكروت كل بناء** | حقن Idempotent + اختبار Byte-for-byte |
| إضافة خطوة بناء تكسر النشر | Snapshot + rollback + Preview أولاً + Gate 1 و2 |
| التقاط ملفات `sync-conflict` | مسارات صريحة · ممنوع glob · نسخة نظيفة |
| حقن HTML/JSON-LD | تعقيم كامل على كل نص ورابط |
| اختفاء الصور أو تغيّرها | نسخ محلي وقت البناء |
| العمل على فرع لا يُنشر | حسم الفرع في المجموعة 1 |
| الكارت يبدو بانراً | الالتزام الحرفي بالمواصفات |

## 10. خارج النطاق

- تشغيل الفيديو داخل الفارس
- أماكن عرض إضافية خارج صفحة الخدمة والرئيسية وصفحتَي المناطق
- تنفيذ `docs/datacodex-cross-link-map.md` — موثّقة لكنها **غير منفّذة في الكود**، وهي عمل منفصل
- **تعديل أي شيء في مشروع داتا كودكس أو أداة النشر — خارج صلاحية هذا المشروع**

## 11. التوثيق بعد التنفيذ

`project-context.md` · `project-key.md` · `changelog.md` + رفع رقم الإصدار.

# انتهى النسخ
