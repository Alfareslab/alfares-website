
# خطة 59 — جسر كروت الحالات من Datacodex

> **الإصدار:** 3.2.0 · **التاريخ:** 2026-09-13
> **الحالة:** **Review Gate 1 مقفول** — Snapshot Cloudflare ومسار الرجوع موثَّقان، التنفيذ يبدأ من المجموعة 2
> **المصدر:** مواصفات مسلَّمة من مشروع Datacodex Content Tool

---

## 0. ما تغيّر عن 3.0.0 (و3.1.0 → 3.2.0)

> كل بند هنا مصدره **حصراً** `docs/reviews/03-review-plan-59-pre-implementation.md` أو
> Snapshot Cloudflare المسجَّل فعلياً — لا قرار مخترَع.

| # | البند | التغيير |
|---|---|---|
| 1 | فرع النشر (مجموعة 1) | ✅ حُسم: `main` (المربوط بـCloudflare Pages) — علّم [x] |
| 2 | نسخة نظيفة (مجموعة 1) | ✅ متحقق فعلاً بخطة 60 — علّم [x] |
| 2b | 🆕 3.2.0 — Snapshot Cloudflare + Rollback | مسجَّلان بالكامل (القسم 7.1/7.2)، وثلاث نتائج مستخلصة (القسم 7.3): `NODE_VERSION` غير معرَّف، `Build watch paths=*` أثر جانبي، `_headers`/`_redirects` لا يحتاجان تعديلاً على `_headers`. **Review Gate 1 مقفول** |
| 3 | حماية المستندات | 🆕 مجموعة جديدة كاملة (القسم 5): `_redirects` يرجّع 404 لـ`/plans/*` و`/docs/*` وملفات md بالجذر |
| 4 | سلوك الحالة الفاضية | توضيح وتوثيق واقع الفيد الحي اليوم (عنصران، كلاهما `post`) وأثره العددي على الصفحات |
| 5 | شكل الرئيسية | تفاصيل معتمدة كاملة: نقطة إدراج مضبوطة بالسطر، 3 أعمدة/كارت عمودي، نقاط الكسر الأربع، انحراف موثَّق + تخفيف ملزم |
| 6 | قاعدة الخصائص المنطقية | 🆕 تحذير صريح: ممنوع أي `[dir]` selector يقلب خاصية منطقية (باج فعلي انصلح في البروتوتايب) |
| 7 | استثناء `target="_blank"` | 🆕 استثناء ثالث معلن لقاعدة «لا تلمس»: تعديل السمة فقط في `index.html:605` و`en/index.html:614` |
| 8 | خريطة المواضيع | عمود تحقق جديد يثبت وجود الصفحات العشر فعلياً |
| 9 | زر الأرشيف | لم يعد مؤجلاً — `/posts/` و`/en/posts/` متحقق منهما 200 اليوم |
| 10 | Flag الانتقالي | خطوة إزالة صريحة مرقّمة في المجموعة الأخيرة، لا ملاحظة جانبية |
| 11 | Deploy Hook | انتقل من المجموعة 7 إلى ما بعد إغلاق الخطة + بند تسليم لوكيل Datacodex |
| 12 | الدستور | بند جديد: توثيق أمر البناء ومجلد الإخراج ونسخة Node في `master-constitution.md` |
| 13 | المخرجات البصرية | `prototype-homepage-strip.html` و`preview-homepage-with-strip.html` معتمدان كمرجع بصري ملزم |

---

## 1. الهدف

صفحات الخدمة في الفارس **خالية حالياً من أي صور ومن أي محتوى متجدد**. هذه الخطة تجعلها تعرض كروتاً لمحتوى موثّق منشور على `datacodexlab.com`، وتتحدث تلقائياً مع كل نشر جديد هناك — **بصفر تدخل يدوي في هذا المشروع بعد التنفيذ**.

## 2. السياق الاستراتيجي — اقرأه قبل أي كود

العلاقة موثّقة رسمياً في `Datacodex/docs/SEO_ARCHITECTURE.md` بنموذج **Hub & Spoke**:

| الطرف | الدور |
|---|---|
| **datacodexlab.com** | المركز — سلطة معرفية رقمية، هوية محمولة غير مقيدة جغرافياً |
| **alfareslab.com** | الطرف — الكيان الجغرافي الفعلي في جدة ونقطة استلام العملاء |

الربط **منفَّذ فعلاً** في `seo/structured-data.json` بهذا المشروع: الموظف معرَّف بـ`https://datacodexlab.com/#ahmed-saleh`، وحسابات التواصل مشتركة، والتذييل يصرّح بالعلاقة.

**اتجاه تدفق السلطة مقصود: من الفارس → داتا كودكس.**

**العدو المُسمّى: تضارب الكلمات المفتاحية (Cannibalization).** لذلك:

> 🚫 **لا يُنسخ نص أي مقال من داتا كودكس إلى هذا الموقع إطلاقاً.**
> الكارت يحمل عنواناً وسطر ملخص وصورة ورابطاً — لا أكثر.

## 3. اكتشاف حاكم عن هذا المشروع

**هذا المشروع لا يملك خطوة بناء إطلاقاً.** لا `package.json`، لا GitHub Actions، لا أي إعداد بناء. ما يجري نشر تلقائي لا بناء.

**قرار المطور:** تُضاف خطوة بناء بسيطة. الموقع يبقى ستاتيك وشكله لا يتغير.

**مرفوض صراحةً:**
- ❌ تحميل الكروت بجافاسكريبت بعد فتح الصفحة — لن تراها محركات البحث، ويلغي الهدف كله
- ❌ التحديث اليدوي — يخالف «صفر تدخل يدوي»
- ❌ **بناء إلى `dist/` بقائمة ملفات مسموحة** — مرفوض بقرار موثَّق: أي ملف جديد لا يُضاف للقائمة يختفي من الإنتاج بصمت. البديل: **تعديل في المكان بأربعة ضمانات** (القسم 6)

---

## 4. مصدر البيانات

موقع داتا كودكس يولّد وقت بنائه ملف `feed.json` — **نقطة التماس الوحيدة**، على العنوان
الثابت:

```
https://datacodexlab.com/feed.json
```

### عقد القائمة — تحقق منه ولا تفترض صحته

| البند | القاعدة |
|---|---|
| `schemaVersion` | **يجب أن يساوي `2` تماماً (رقم صحيح)** — أي قيمة أخرى، غائبة أو نصية أو مختلفة، تُفشل البناء |
| شكل الجذر | كائن يحمل `schemaVersion` و`items[]` |
| `id` | نص فريد ومستقر لكل عنصر — **لا يُستخدم للترتيب ولا للعرض**، فقط لإزالة التكرار دفاعياً (انظر سياسة الفشل، القسم 5) |
| `title` | نص |
| `cardSummary` | سطر مكتوب خصيصاً للكارت — **ليس منقولاً من المقال** |
| `image` | مطلق · HTTPS · نطاق `datacodexlab.com` |
| `url` | مطلق · HTTPS · نفس النطاق · **بشرطة مائلة ختامية** |
| `topics[]` | مصفوفة نصوص غير فارغة، **بحد أقصى عنصرين**، تصف **موضوع المحتوى** — لا تحمل أي معرّف صفحة ولا أي علم بوجود موقع الفارس |
| `lang` | `ar` \| `en` |
| `type` | `post` \| `case` \| `service` |
| `publishedAt` | ISO 8601 بـUTC |
| `hasVideo` | منطقي |

**مثال حقيقي — عنصر واحد كما هو منسوخ حرفياً من الفيد الحي بتاريخ 2026-09-13** (توضيحي
لشكل العنصر فقط، **ليس قائمة معتمدة ولا مرجعاً دائماً** — القيم الفعلية تتغير مع كل نشر
جديد على داتا كودكس):

```json
{
  "id": "post:en:klevv-neo-n400-ssd-recovery",
  "title": "KLEVV NEO N400 SSD Data Recovery: Resolving SM2258XT Firmware Panic Protocols",
  "cardSummary": "KLEVV NEO N400 SSD Data Recovery | SM2258XT Firmware Fix",
  "image": "https://datacodexlab.com/_astro/klevv-neo-n400-ssd-recovery.B3OCTYRi_q35KR.webp",
  "url": "https://datacodexlab.com/en/posts/en-klevv-neo-n400-ssd-recovery/",
  "topics": ["hdd-internal", "ssd-nvme"],
  "lang": "en",
  "type": "post",
  "publishedAt": "2026-08-30T00:00:00.000Z",
  "hasVideo": false
}
```

🛑 **يُفشل البناء كله مع تسجيل السبب:** عنصر يخالف **شكل** العقد (مثال: `topics[]` ليست
مصفوفة نصوص، أو فارغة، أو تحمل أكثر من عنصرين) · رابط خارج النطاق أو غير HTTPS · `url`
مكرر · عنصر ناقص حقلاً مطلوباً · `schemaVersion` لا يساوي `2`.

> ⚠️ **لا تستبعد هذه العناصر وتكمل البناء.** الاستبعاد الصامت ينشر موقعاً ناقصاً بلا إنذار.
> **الاستثناءان الموثقان الوحيدان من هذه القاعدة: تكرار `id`، ومعرّف موضوع صالح الشكل
> لكنه غير موجود في جدول هذا القسم** — كلاهما موثّق بدقة في سياسة الفشل، القسم 5، ولا
> يُسقَط أي منهما صامتاً، بل بتسجيل صريح في سجل البناء. لا يُوسَّع أي منهما لأي حالة
> أخرى.
>
> **مهم:** معرّف موضوع صالح الشكل (نص، ضمن حد العنصرين) لكنه غير موجود في جدول
> "خريطة المواضيع إلى صفحات الفارس" أدناه **ليس مخالفة عقد ولا يُفشل البناء إطلاقاً.**
> هذا **متوقع تماماً** إن أضافت داتا كودكس مستقبلاً قيمة جديدة لقائمتها المغلقة قبل أن
> يعرف بها هذا الجدول — الفيد يبقى صالحاً بعقده، والعلاج مراجعة الجدول أدناه لا إصلاح
> شيء في السكربت. السلوك المطلوب: استبعاد طبيعي للعنصر من أي صفحة، مع **تسجيل تحذير
> صريح في سجل البناء يذكر المعرّف غير المعروف بالاسم** — تفصيل كامل في القسم 5.

### خريطة المواضيع إلى صفحات الفارس

**هذا الجدول ملك لمشروع الفارس وحده. الفيد لا يعرف بوجوده ولا يلتزم بمزامنته.**
الفيد يرسل `topics[]` — موضوع محتوى بحت، لا معرّف صفحة. الربط بين موضوع وصفحة خدمة فعلية
في الفارس **قرار يخص هذا المشروع حصراً**، يُصان بالكامل هنا داخل السكربت (القسم 7،
المجموعة 2). لو تغيّرت القائمة المغلقة للمواضيع مستقبلاً — بالإضافة أو الحذف — فلن يصل
أي إخطار تلقائي؛ مراجعة هذا الجدول عند كل تحديث لهذا الملف مسؤولية وكيل الفارس.

**المعرّف الإنجليزي هو ما يُطابَق عليه.**

✅ **اعتمدت كما هي بمراجعة 03** — عمود التحقق يثبت أن الصفحات العشر موجودة فعلاً عربي
وإنجليزي، وأن صفحتَي مكة والسعودية خارج نطاق المطابقة عمداً (القسم أدناه).

| المعرّف | الصفحة العربية | الصفحة الإنجليزية | تحقق الوجود (2026-09-13) |
|---|---|---|---|
| `hdd-internal` | `services/hdd-data-recovery.html` | `en/services/hdd-data-recovery.html` | ✅ موجودتان |
| `hdd-external` | `services/external-hdd-data-recovery.html` | `en/services/external-hdd-data-recovery.html` | ✅ موجودتان |
| `ssd-nvme` | `services/ssd-nvme-data-recovery.html` | `en/services/ssd-nvme-data-recovery.html` | ✅ موجودتان |
| `laptop-pc` | `services/laptop-pc-data-recovery.html` | `en/services/laptop-pc-data-recovery.html` | ✅ موجودتان |
| `mac` | `services/mac-data-recovery.html` | `en/services/mac-data-recovery.html` | ✅ موجودتان |
| `flash-sd` | `services/flash-sd-data-recovery.html` | `en/services/flash-sd-data-recovery.html` | ✅ موجودتان |
| `raid-nas` | `services/raid-nas-data-recovery.html` | `en/services/raid-nas-data-recovery.html` | ✅ موجودتان |
| `dvr-nvr` | `services/dvr-nvr-data-recovery.html` | `en/services/dvr-nvr-data-recovery.html` | ✅ موجودتان |
| `ransomware` | `services/ransomware-data-recovery.html` | `en/services/ransomware-data-recovery.html` | ✅ موجودتان |
| `database-erp` | `services/database-erp-recovery.html` | `en/services/database-erp-recovery.html` | ✅ موجودتان |

**قاعدة المطابقة: احتواء مصفوفة، لا مساواة.** عنصر يُعرَض على صفحة خدمة إذا كان
`topics[]` **يحتوي** معرّف تلك الصفحة (`topics.includes(pageTopicId)`) — بصرف النظر
عن أي قيمة أخرى موجودة معه في نفس المصفوفة.

**عنصر واحد يحمل موضوعين يظهر على صفحتَي خدمة مختلفتين في آنٍ واحد — هذا سلوك متوقع
وطبيعي، وهو بالضبط المقصود من سقف العنصرين في العقد، وليس خطأ بيانات ولا تكراراً
يجب إصلاحه.** الفيد الحي يستخدم هذه الحالة فعلياً اليوم: عنصرا `klevv-neo-n400-ssd-recovery`
(عربي وإنجليزي) يحملان `topics: ["hdd-internal", "ssd-nvme"]` ويظهران على صفحة الهارد
الداخلي وصفحة SSD/NVMe معاً.

**صفحتا المناطق** (`data-recovery-makkah` · `data-recovery-saudi-arabia`): لا تُطابق على معرّف — تعرضان **أحدث المحتوى الموجود في القائمة** بصرف النظر عن الصفحة.

**`about-lab` و`privacy-policy` مستبعدتان تماماً.**

### الترتيب

> **عضوية الصفحة هي الصلة** (فلترة نعم/لا)، **ثم الترتيب بالحداثة**.

القائمة لا تحمل درجة صلة — **لا تخترع ترتيباً**. في الرئيسية: الحداثة أولاً.

---

## 5. سياسة الفشل — اقرأها بعناية

هذا أهم قرار تشغيلي في الخطة.

| الحالة | السلوك |
|---|---|
| قائمة صالحة بلا عناصر مطابقة لهذه الصفحة | ✅ **بناء ناجح بلا كروت** |
| عنصر **مطابق للعقد** لكنه لا يطابق لغة الصفحة أو لا يحتوي `topics[]` على معرّف هذه الصفحة | ✅ **استبعاد طبيعي من تلك الصفحة فقط** — البناء يكمل |
| عنصر يحمل معرّف موضوع **صالح الشكل** (نص ضمن حد العنصرين) لكنه **غير موجود في جدول "خريطة المواضيع إلى صفحات الفارس"** (القسم 4) | ✅ **استبعاد طبيعي من كل الصفحات، بلا إفشال بناء** — **يُسجَّل صراحة في سجل البناء بالمعرّف غير المعروف باسمه**. هذا **متوقع** عند توسّع القائمة المغلقة لدى داتا كودكس قبل تحديث هذا الجدول — العلاج مراجعة الجدول لا إصلاح السكربت. هذه ليست مخالفة عقد؛ العنصر صالح تماماً بعقده الأصلي |
| عنصران يحملان نفس `id` | ✅ **إسقاط دفاعي للنسخة المكررة فقط، بلا إفشال بناء** — **يُسجَّل صراحة في سجل البناء بالمعرّف المكرر** وسبب الإسقاط، وتُبقى النسخة الأولى المصادَفة. هذا استثناء موثَّق من قاعدة "لا إسقاط صامت" أدناه: الممنوع هو **الصمت**، لا الإسقاط — التسجيل الصريح شرط ملزم لا اختياري، ولا يُوسَّع هذا الاستثناء لأي حالة أخرى |
| **فشل تنزيل أو التحقق من صورة عنصر صالح** | 🛑 **أفشل البناء** |
| فشل شبكة · HTTP غير ناجح · JSON تالف · مخالفة العقد · `schemaVersion` لا يساوي `2` | 🛑 **أفشل البناء** |

**السبب:** لو بنيت بنجاح بلا كروت عند فشل الشبكة، فأي انقطاع مؤقت **يمحو كل الكروت من الموقع الحي وينشر ذلك** — ومحدش يلاحظ لأن البناء «نجح». إفشال البناء يُبقي آخر Deployment سليماً.

> **القاعدة الفاصلة:** الاستبعاد الطبيعي مسموح **فقط** بعد نجاح تحقق العقد، ولثلاثة أسباب حصراً: عدم مطابقة اللغة، عدم مطابقة الصفحة، أو معرّف موضوع صالح الشكل غير معروف محلياً (مسجَّل صراحة). تكرار `id` معالَج صراحة أعلاه كاستثناء موثَّق مسجَّل صراحة في سجل البناء. كل ما عدا هذه الحالات — فشل بناء، **لا إسقاط صامت لأي عنصر آخر**.

- [ ] [🤖] timeout صريح للطلب
- [ ] [🤖] رسالة خطأ واضحة بلا أسرار
- [ ] [🤖] **Flag انتقالي مؤقت** يسمح بالبناء بلا قائمة **قبل الإطلاق الأول فقط** — يُزال بخطوة صريحة مرقّمة في المجموعة 8 فور نجاح أول نشر على Production (انظر القسم 8.ح)

### 5.1 سلوك الحالة الفاضية — بند حاسم ✅ معتمد بمراجعة 03

**صفر عناصر مطابقة = لا يُطبع أي HTML إطلاقاً.** لا عنوان ولا إطار ولا سطر تحتاني ولا
زر ولا تعليق. **ليس `display:none`** بل غياب كامل من المصدر — كأن شيئاً لم يكن. ينطبق
على صفحات الخدمة **وشريط الرئيسية معاً**، بلا استثناء.

⚠️ **واقع الفيد اليوم (تحقق مباشر بتاريخ 2026-09-13 من `https://datacodexlab.com/feed.json`):**
الفيد يحمل **عنصرين فقط**، الاثنان بنفس المضمون (نسخة عربية ونسخة إنجليزية)، والاثنان
`type: "post"` — **لا يوجد ولا عنصر واحد `type: "case"`**. هذا يعني عند أول نشر:

- **10 من 12 صفحة خدمة تبني فاضية تماماً** — فقط صفحتا `hdd-internal` و`ssd-nvme`
  (عربي وإنجليزي) تحصلان على كارت واحد لكل لغة، لأن العنصر الوحيد المتاح يحمل
  `topics: ["hdd-internal", "ssd-nvme"]`
- **شريط الرئيسية يبني فاضياً تماماً** — لأن المجموعة 6 تفلتر على `type = case` حصراً،
  ولا عنصر واحد بهذا النوع موجود اليوم

**هذا سلوك صحيح ومتوقع تماماً، وليس خطأً في السكربت ولا في الفيد.** لا يُعالَج بأي
Workaround — الحل الوحيد هو نشر Datacodex لمزيد من المحتوى مستقبلاً.

---

## 6. الضمانات الأربعة للتعديل في المكان

| # | الضمان |
|---|--------|
| 1 | **حقن Idempotent** — استبدال المنطقة بين علامة بداية وعلامة نهاية، لا الإضافة إليها |
| 2 | **مسارات ملفات صريحة مُعدَّدة في السكربت — ممنوع أي glob** |
| 3 | **تشغيلان متتاليان ينتجان ملفات متطابقة Byte-for-byte** — اختبار قبول إلزامي |
| 4 | **لا Commit لأي HTML مولَّد** — نسخة Cloudflare مؤقتة |

> ⚠️ الضمان 2 ليس شكلياً: المستودع يحتوي عدداً كبيراً من ملفات `sync-conflict` غير المتتبعة. أي glob سيلتقطها.

---

## 7. خطوات التنفيذ

### المجموعة 1: التحضير والحماية

- [x] [🤖] **ابدأ من نسخة نظيفة** — متحقق بخطة 60 (`chore(repo): clean working tree`). الإدخالات غير المتتبعة الحالية (`docs/research/`، `docs/reviews/03-review-plan-59-pre-implementation.md`) هي بالذات وثائق تحضير هذه الخطة، لا اتساخاً خارجياً
- [x] [🤖] **احسم فرع النشر صراحةً** — **`main`** هو فرع النشر المربوط بـCloudflare Pages، ومطابق لـ`origin/HEAD`. لا تعمل على `master`
- [x] [👤] **المطور:** تسجيل Snapshot لإعداد Cloudflare — مسجَّل بالكامل في القسم 7.1 أدناه
- [x] [🤖] توثيق **مسار rollback دقيق** خطوة بخطوة — موثَّق في القسم 7.2 أدناه

### 7.1 Snapshot إعدادات Cloudflare — مسجَّل 2026-09-13

| الإعداد | القيمة وقت التسجيل |
|---|---|
| المشروع | `alfares-website` |
| الريبو | `Alfareslab/alfares-website` |
| النطاقات | `alfareslab.com` · `www.alfareslab.com` · `alfares-website.pages.dev` |
| Production branch | `main` |
| Automatic deployments | Enabled |
| Build command | (فارغ) |
| Build output | (فارغ = الجذر) |
| Root directory | (فارغ = الجذر) |
| Build cache | Disabled |
| Build comments | Enabled |
| Build watch paths | `*` |
| Build system version | Version 3 |
| Deploy Hooks | لا يوجد |
| Variables and secrets | **فارغة تماماً — لا `NODE_VERSION` معرَّف** |
| Bindings / Pages Functions | لا يوجد |
| Placement | off |
| Compatibility date | 2025-11-05 |
| Fail mode | Fail open |
| Preview branch | All non-production branches |
| Preview access | عام افتراضياً |

### 7.2 مسار الرجوع (Rollback) — محقَّق

Cloudflare يحتفظ بكل الـdeployments السابقة على هذا المشروع — تحقق مباشر: `a3dbf00`
و`e815543` لا يزالان متاحين في قائمة الـDeployments وقت التسجيل.

**الإجراء:** لوحة Cloudflare → Deployments → اختيار الـdeployment السليم السابق → ⋯ →
**Rollback**. الرجوع **فوري ولا يحتاج أي عملية Git** (لا revert ولا push) — هذا هو
**مسار الرجوع الأساسي المعتمد لهذه الخطة**، ويُستخدم فوراً لو أي نشر بعد إضافة خطوة
البناء كسر الموقع الحي.

### 7.3 ثلاث نتائج مستخلصة من الـSnapshot

أ) **`NODE_VERSION` غير معرَّف كمتغير بيئة.**
   - [ ] [👤] **المطور:** إضافة `NODE_VERSION` كمتغير بيئة عند تنفيذ إعداد Cloudflare
     الفعلي (لا الآن) — لتثبيت نسخة Node صراحةً، لأن الاعتماد على افتراضي Build system
     v3 عرضة لتغيير من طرف Cloudflare دون إشعار مسبق لهذا المشروع

ب) **Build watch paths = `*`.**
   بعد إضافة أمر البناء، **أي `push`** — حتى توثيق بحت لا يمس كوداً — سيُشغّل
   `scripts/build-cards.mjs` ويجلب `feed.json`، وأي فشل في الفيد أو الشبكة وقتها
   **سيُفشل ذلك البناء تحديداً**، بصرف النظر عن كون الـpush نفسه لا علاقة له بالكروت.
   هذا **مقصود** ومتسق مع سياسة الفشل (القسم 5) — **موثَّق هنا صراحةً كأثر جانبي معروف**،
   لا كخلل يحتاج معالجة

ج) **`_headers` موجود وCSP فيه يسمح بالمطلوب فعلاً.**
   `img-src` يشمل `'self'` و`https:`، و`style-src` يشمل `'self'` — فملف CSS الجديد
   والصور المحلية الجديدة في `assets/images/datacodex-cards/` تمر **بلا أي تعديل على
   `_headers`**. **لا تعديل مطلوب على هذا الملف إطلاقاً.**
   **`_redirects` غير موجود إطلاقاً حالياً** — إنشاؤه في المجموعة 2 **إضافة لا استبدال**،
   والروابط النظيفة الحالية (`/services/hdd-data-recovery.html` بلا امتداد ظاهر إلخ)
   تعمل اليوم بسلوك Cloudflare Pages التلقائي دون أي قاعدة صريحة — لذلك **التحقق على
   Preview قبل Production يبقى إلزامياً** كما هو مسجَّل في المجموعة 2، فلا شيء هنا يُسقط
   هذا الشرط

### ✅ المجموعة 1 مكتملة — Review Gate 1 مقفول

جميع بنود المجموعة 1 معلَّمة، والـSnapshot ومسار الرجوع موثَّقان بالكامل أعلاه.
**التنفيذ يبدأ من المجموعة 2 (حماية المستندات من النشر).**

⏸️ ~~**Review Gate 1** — لا شيء يبدأ قبل وجود Snapshot ومسار رجوع.~~ **مقفول 2026-09-13.**

### المجموعة 2: حماية المستندات من النشر 🆕

> **الواقع المتحقق (2026-09-13):** `alfareslab.com/plans/*.md` و`/project-context.md`
> و`/changelog.md` ترجع **200** لأي زائر مجهول — لأن Cloudflare Pages ينشر الريبو كله
> كملفات ستاتيك بلا تمييز. مثال تحقق حي: `alfareslab.com/plans/59-datacodex-cards-bridge.md`
> يرجع 200 اليوم.

- [ ] [🤖] إضافة/تعديل `_redirects` بقواعد ترجع **404** صراحة لـ:
  - `/plans/*`
  - `/docs/*`
  - كل ملفات `*.md` في جذر الموقع (`/*.md`)
- [ ] [🤖] **لا تلمس** أي قاعدة `_redirects` قائمة تخص مساراً آخر — إضافة فقط

⚠️ **قاعدة `_redirects` غلط ممكن تكسر مسارات الموقع أو يتجاهلها Cloudflare بصمت.**
لذلك:

- [ ] [🤖] **التحقق إلزامي على Preview قبل Production** — لا استثناء
- [ ] [🤖] اختبار أن **صفحة حقيقية من كل نوع** لسه ترجع 200 بعد إضافة القواعد: الرئيسية
      (`index.html`)، صفحة خدمة عربي (مثال `services/hdd-data-recovery.html`)، صفحة
      خدمة إنجليزي (مثال `en/services/hdd-data-recovery.html`)
- [ ] [🤖] اختبار أن `/plans/*` و`/docs/*` وملف md بالجذر أصبحت **404** فعلاً على نفس الـPreview

⏸️ **Review Gate 2** — لا انتقال لـProduction قبل تأكيد الأربع نقاط أعلاه على Preview.

### المجموعة 3: السكربت

- [ ] [🤖] `scripts/build-cards.mjs` — **بدون أي مكتبة خارجية**، Node المدمج فقط
- [ ] [🤖] جلب القائمة والتحقق من العقد بالكامل قبل استخدامها
- [ ] [🤖] الفلترة باللغة واحتواء `topics[]` على معرّف الصفحة (`topics.includes(pageTopicId)`)، ثم الترتيب بالحداثة
- [ ] [🤖] تطبيق سياسة الفشل (القسم 5) والضمانات الأربعة (القسم 6)
- [ ] [🤖] **تنزيل الصور محلياً** إلى `assets/images/datacodex-cards/` — مستبعدة من Git، تُولّد وقت البناء. تحقق من النوع والحجم الأقصى. 🛑 **فشل التنزيل أو التحقق يُفشل البناء** — **ممنوع إسقاط العنصر ومواصلة النشر** (فشل صامت جزئي يخفي كارتاً من الموقع الحي)، و**ممنوع Hotlink بديلاً**
- [ ] [🤖] **تعقيم كامل:** HTML-escape لكل نص · Attribute-escape داخل السمات · التحقق من بروتوكول ونطاق كل رابط · منع `javascript:` · JSON-LD عبر `JSON.stringify` مع منع `</script>` من إنهاء الكتلة
- [ ] [🤖] **سجل تشغيل:** عدد العناصر المستلمة · المحقونة لكل صفحة · سبب كل استبعاد · معرّف أي عنصر أُسقط بسبب تكرار `id` · **أي معرّف موضوع غير معروف صودف أثناء المطابقة، بالاسم**. بدونه تفشل الحزمة بصمت رغم نجاح البناء
- [ ] [🤖] تشغيل محلي + **اختبار التطابق Byte-for-byte**

### المجموعة 4: علامات الموضع

- [ ] [🤖] **علامتان** في كل صفحة خدمة: واحدة **تحت المقدمة** (بعد فقرتَي التمهيد وقبل أول `<h2>`)، وواحدة **قبل دعوة التواصل**
- [ ] [🤖] 12 صفحة عربية + 12 إنجليزية في `en/services/`
- [ ] [🤖] كل علامة تحمل معرّف صفحتها وحدَّي بداية ونهاية للمنطقة المولَّدة
- [ ] [🤖] **التوزيع المثبَّت: 1 بعد المقدمة + 2 في النهاية** — وحدِّد سلوكاً صريحاً عند توفر 0 أو 1 أو 2 عناصر فقط (0 عناصر ⇐ القسم 5.1 حرفياً: غياب كامل من المصدر)
- [ ] [🤖] **لا تلمس أي شيء آخر في هذه الصفحات** عدا سطرَي العلامة — **ما عدا الاستثناءات المعلنة الثلاثة** أدناه

> **الاستثناء المعلن الأول:** لك أن تضيف ملف CSS جديد وتربطه في `<head>` هذه الصفحات.
>
> **الاستثناء المعلن الثاني:** لك أن تعدّل الرئيسية وصفحتَي المناطق طبقاً للمجموعة 6.
>
> **الاستثناء المعلن الثالث 🆕:** الرئيسية (وليست صفحات الخدمة) تحمل رابطاً قائماً إلى
> `https://datacodexlab.com/posts/` عند `index.html:605` و`en/index.html:614`، ويستخدم
> `target="_blank"` — وهذا يخالف القاعدة الجديدة الممنوعة للكروت (المجموعة 5 أدناه).
> **القرار المعتمد:** يُبقى الزران معاً — الرابط القديم وزر الأرشيف الجديد — ويُعدَّل
> الرابط القديم **بإزالة السمة `target="_blank"` فقط، ولا شيء غيرها** في هذين السطرين
> تحديداً. ما عدا هذه السمة، لا تلمس أي شيء آخر في هذين السطرين.

⏸️ **Review Gate 3** — اعرض السكربت والناتج المحلي. **لا تلمس إعدادات Cloudflare قبل الموافقة.**

### المجموعة 5: تصميم الكارت (صفحات الخدمة)

التصميم المعتمد: **«مرجع داخل المتن»** — اختير بعد بحث خارجي ومعاينة ثلاثة بدائل.

> **القاعدة الحاكمة: الكارت يجب أن يبدو مُتعمَّداً ألا يبدو كبطاقة محتوى.**
> لغة بصرية **استشهادية/مرجعية**، لا بانر. السبب: العنصر داخل صفحة تجارية ويقود إلى نطاق آخر — لو ظهر كبانر وقع في المنطقة الذهنية التي يتجاهل فيها المستخدم الإعلانات.

- [ ] [🤖] **Desktop:** أفقي — صورة **34%** / محتوى **66%**
- [ ] [🤖] **بلا ظل ثقيل وبلا خلفية ملوّنة.** `border: 1px` هادئ + **شريط رأسي 3–4px عند `inline-start`** — لا تحذفه، هو ما يعطي إحساس المرجع
- [ ] [🤖] الترتيب: `سطر تمهيدي ثابت → العنوان → الملخص → التاريخ → CTA`
- [ ] [🤖] السطر التمهيدي: **«من توثيق أعمالنا على Datacodex»** / `From our documented work on Datacodex`
- [ ] [🤖] الأحجام: تمهيدي `0.75–0.8rem` · عنوان `1.15–1.3rem` وزن 650–700 · ملخص `0.95–1rem` · تاريخ `0.8–0.85rem`
- [ ] [🤖] Padding `20–24px` · عنوان↔ملخص `8–10px` · ملخص↔تذييل `14–16px`
- [ ] [🤖] **الموبايل (< 640–700px): صف وسائط لا stack** — صورة 32–36% على `inline-start` والنص 64–68%. استثناء عند `< 380px` فقط: الصورة 100% فوق النص
- [ ] [🤖] **Hover:** ممنوع رفع الكارت. المسموح: تغيّر خفيف في `border`/`background` + `underline` للـCTA + `scale(1.015)` للصورة داخل `overflow: hidden` بمدة `150–180ms`
- [ ] [🤖] احترام `prefers-reduced-motion: reduce`
- [ ] [🤖] **خصائص CSS منطقية حصراً** — نفس الـDOM يخدم RTL وLTR
- [ ] [🤖] استخدام متغيرات `assets/css/base.css` — والعمل في الوضعين الفاتح والداكن
- [ ] [🤖] الصورة `aspect-ratio: 16/9` و`object-fit: cover` والمساحة محجوزة مسبقاً

> ⚠️ **قاعدة صارمة 🆕 (باج فعلي انصلح في البروتوتايب):** **ممنوع أي selector من نوع
> `[dir="ltr"]` أو `[dir="rtl"]` يقلب خاصية منطقية** (مثل تحويل `margin-inline-start`
> إلى `margin-right` يدوياً حسب الاتجاه). الخاصية المنطقية تنقلب تلقائياً مع اتجاه
> المستند — أي `[dir]` override فوقها يكرر العمل ويكسره. هذا كان خطأً فعلياً في
> `prototype-homepage-strip.html` (الكلاس `.doc-card`) واتصلح؛ لا يتكرر هنا.

**المرجع البصري الجاهز:** `DataCodex_Content_Tool/docs/research/prototypes/prototype-01-inline-reference.html`

### المجموعة 6: قواعد ملزمة (نتائج بحث خارجي)

- [ ] [🤖] `<a href>` عادي — **بدون `rel="nofollow"`**. إرشادات Google: الرابط التحريري الطبيعي لا يحتاجه، وإضافته تناقض هدف المشروع
- [ ] [🤖] **بدون `target="_blank"`** — W3C وGOV.UK: فتح تبويب دون تحذير يربك المستخدم
- [ ] [🤖] **ممنوع زر تشغيل دائري كبير** في منتصف الصورة. البديل: badge صغيرة في الركن `▶ فيديو على Datacodex`. السبب: الفيديو لا يُشغَّل هنا والزر الكبير وعد كاذب
- [ ] [🤖] الاسم المتاح لقارئ الشاشة يتضمن وجود الفيديو وأن الوجهة Datacodex
- [ ] [🤖] **ممنوع «اقرأ المزيد»** — المعتمد: **«اطّلع على التوثيق الكامل على Datacodex»** / `View the full documentation on Datacodex`
- [ ] [🤖] **ممنوع تدوير الكروت عشوائياً** لتغيير الـHTML — يقع تحت سياسات Google للتلاعب
- [ ] [🤖] **JSON-LD — تحسين ثانوي لا بوابة:** `ItemList → ListItem → CreativeWork` · `publisher` = **Datacodex** · **ممنوع** `author: Alfares` · **ممنوع** `mainEntityOfPage` يشير للفارس. الفارس **يستشهد ولا يدّعي الملكية**. أضف اختبارات تثبت ذلك
- [ ] [🤖] زر «استكشف سجل التوثيق الكامل على Datacodex» — ✅ **لم يعد مؤجلاً.** تحقق حي
      بتاريخ 2026-09-13: `https://datacodexlab.com/posts/` و`https://datacodexlab.com/en/posts/`
      كلاهما يرجع **200**. أضِفه في الرئيسية وصفحات الخدمة كما هو معتمد
- [ ] [🤖] سطر صغير أسفل كل مجموعة: «من مدونتنا التقنية — داتا كودكس لاب»

### المجموعة 7: الرئيسية وصفحات المناطق

**المرجع البصري الملزم للتنفيذ 🆕 — معتمد بمراجعة 03:**
`docs/research/prototype-homepage-strip.html` و`docs/research/preview-homepage-with-strip.html`.
كلاهما اجتاز فحوصات التزام آلية (بلا `box-shadow`، خصائص منطقية حصراً، الشريط الرأسي
موجود، بلا رفع للكارت عند hover، بلا `target="_blank"`، بلا `rel="nofollow"`، CTA
المعتمد، `prefers-reduced-motion`، `aspect-ratio: 16/9`) والتحقق المستقل أن
`index.html`/`en/index.html`/`assets/` لم تُمس فيهما إطلاقاً (ملفات معاينة فقط).

- [ ] [🤖] شريط **«أحدث ما وثّقناه»** في `index.html` و`en/index.html`
- [ ] [🤖] **موضع الإدراج الدقيق (متحقق منه بالسطر، 2026-09-13):**
      - `index.html`: بعد `</section>` الخدمات في **السطر 664**، وقبل تعليق
        `<!-- Capabilities Section -->` في **السطر 666**
      - `en/index.html`: بعد `</section>` الخدمات في **السطر 673**، وقبل تعليق
        `<!-- Capabilities Section -->` في **السطر 675**
      - الملفان بنفس التركيب فالحقن متماثل. **تحقق من رقم السطر بنفسك وقت التنفيذ**
        قبل الحقن — قد يتغيّر لو تعدّل الملف بينهما
- [ ] [🤖] **آخر 3 حالات نجاح فقط** — فلترة على `type = case`؛ المقالات مستبعدة هنا.
      ⚠️ **بالفيد الحالي هذا يعني شريطاً فاضياً تماماً** (القسم 5.1) — سلوك صحيح متوقع
- [ ] [🤖] في الرئيسية **الحداثة أولاً**
- [ ] [🤖] صفحتا المناطق: أحدث المحتوى الموجود في القائمة، بلا تعليم يدوي

**شكل الرئيسية في الشاشات العريضة — معتمد بصرياً 🆕:**

- [ ] [🤖] **3 أعمدة جنب بعض، والكارت عمودي** (صورة فوق، نص تحت) في الشاشات العريضة —
      اختيار المطور، يخالف شكل صفحات الخدمة عمداً
- [ ] [🤖] **نقاط الكسر الأربع المعتمدة:**

  ```
  < 380px       →  عمود واحد · كارت عمودي (صورة فوق بعرض كامل)
  380 – 699px   →  عمود واحد · كارت أفقي صورة 34% / نص 66%
  700 – 999px   →  عمودين · كارت عمودي
  ≥ 1000px      →  3 أعمدة · كارت عمودي
  ```

> ⚠️ **انحراف موثَّق عن المواصفة — الرئيسية فقط:** القاعدة الحاكمة في المجموعة 5
> («الكارت يجب أن يبدو مُتعمَّداً ألا يبدو كبطاقة محتوى») تصف كارت صفحة الخدمة. ثلاثة
> كروت عمودية بصورة أعلى كل واحد هي الشكل الكلاسيكي لبطاقة المحتوى، وهذا **ظاهرياً**
> يخالفها. **لكن المواصفة لم تحسم شكل الرئيسية أصلاً** — فراغ حسمه المطور، لا مخالفة.
>
> **التخفيف الملزم عند التنفيذ:** الكارت العمودي في الرئيسية **يحتفظ إلزامياً** بـ:
> - الشريط الرأسي **3–4px** عند `inline-start`
> - الإطار الهادئ **1px**
> - **انعدام الظل والخلفية الملوّنة**
>
> حتى يبقى محتفظاً باللغة المرجعية رغم الشكل العمودي.
>
> **صفحات الخدمة لا تتأثر بهذا الانحراف إطلاقاً** — تبقى بالكارت الأفقي 34/66 «مرجع
> داخل المتن» كما اعتُمد في المجموعة 5.

⏸️ **Review Gate 4**

### المجموعة 8: التشغيل الآلي والتحقق

- [ ] [🤖] **أول نشر على Preview URL** بنفس إعدادات Production قبل تحويل Production
- [ ] [🤖] توثيق أمر البناء ومجلد الإخراج ونسخة Node المطلوبة
- [ ] [🤖] توثيق آلية إعادة البناء **الحالية**: **إعادة بناء عند كل `git push` فقط** —
      Deploy Hook مؤجَّل لما بعد إغلاق الخطة (انظر القسم 8.ط)
- [ ] [🤖] **تحقق ما بعد النشر:** AR و EN · الموضع الأول والأخير · `_headers` · الروابط النظيفة · `canonical`/`hreflang` · الصفحات التي كانت تحت المراقبة في GSC
- [ ] [🤖] **تحقق إضافي 🆕:** `_redirects` لا تزال تُرجع 404 لـ`/plans/*` و`/docs/*` وملفات md بالجذر على Production نفسه، وكل صفحة حقيقية (رئيسية/خدمة عربي/خدمة إنجليزي) لسه 200
- [ ] [🤖] التأكد أن الكارت في **مصدر الصفحة** لا محمَّلاً بجافاسكريبت

> ⚠️ **ضبط لوحة Cloudflare عمل يدوي يخص المطور** — وثّق الخطوات فقط.

**8.ح — إزالة الـFlag الانتقالي (خطوة صريحة، لا ملاحظة جانبية) 🆕**

- [ ] [🤖] **فور نجاح أول نشر على Production** (بعد Review Gate 5): أزل الـFlag الانتقالي
      المذكور في القسم 5 من السكربت تماماً — إبقاؤه بعد هذه اللحظة يلغي أهم حماية في
      الخطة (أن انقطاعاً مؤقتاً في الفيد لا يمحو الكروت من الموقع الحي)

⏸️ **Review Gate 5** — نهائية. E2E حقيقي: نشر على داتا كودكس ← ظهور الكارت في الفارس.

**8.ط — Deploy Hook (مؤجَّل لما بعد إغلاق الخطة) 🆕**

> **قرار المطور:** الـDeploy Hook معتمد مبدئياً، لكن **ينتقل من المجموعة 8 إلى ما بعد
> إغلاق هذه الخطة** كمهمة تسليم منفصلة. حتى ذلك الحين: إعادة البناء تجري عند كل
> `git push` فقط (كما هو موثَّق أعلاه).

- [ ] [🤖] بعد إغلاق هذه الخطة رسمياً: كتابة رسالة تسليم لوكيل مشروع Datacodex توضح
      المطلوب منه بالضبط لتركيب الـDeploy Hook (أين يوضع الرابط السري من Cloudflare
      داخل مشروع Datacodex، ومتى يُستدعى — بعد نجاح بناء Datacodex، لا لحظة الـcommit)

**8.ي — الدستور 🆕**

- [ ] [🤖] توثيق خطوة البناء في `master-constitution.md`: أمر البناء
      (`node scripts/build-cards.mjs`) · مجلد الإخراج (**الجذر** — السلوك القائم فعلاً
      اليوم، لا يضيف خطراً) · نسخة Node المطلوبة. **الجديد الوحيد هو وجود Build command
      أصلاً**: فشله يوقف النشر كله، وهذا مقصود

---

## 8. الملفات المتأثرة

| الملف | التغيير |
|---|---|
| `scripts/build-cards.mjs` | جديد |
| ملف CSS للكارت + ربطه في `<head>` | جديد |
| `services/*.html` (12) | تعديل — سطرا علامة فقط |
| `en/services/*.html` (12) | تعديل — سطرا علامة فقط |
| `index.html` · `en/index.html` | تعديل (شريط الرئيسية + إزالة `target="_blank"` من رابط `/posts/` القائم فقط) |
| `services/data-recovery-makkah.html` · `data-recovery-saudi-arabia.html` (+EN) | تعديل |
| `_redirects` | 🆕 تعديل — قواعد 404 لـ`/plans/*` و`/docs/*` وملفات md بالجذر |
| `.gitignore` | تعديل — استبعاد صور الكروت المولَّدة |
| `master-constitution.md` | 🆕 تعديل — توثيق خطوة البناء |
| إعداد Cloudflare | **يدوي — المطور** |

## 9. المخاطر

| الخطر | التخفيف |
|---|---|
| **فشل مؤقت يمحو الكروت من الموقع الحي** | البناء يفشل ويبقى آخر Deployment سليم |
| **تكرار الكروت كل بناء** | حقن Idempotent + اختبار Byte-for-byte |
| إضافة خطوة بناء تكسر النشر | Snapshot + rollback + Preview أولاً + Gate 1 و2 |
| التقاط ملفات `sync-conflict` | مسارات صريحة · ممنوع glob · نسخة نظيفة |
| حقن HTML/JSON-LD | تعقيم كامل على كل نص ورابط |
| اختفاء الصور أو تغيّرها | نسخ محلي وقت البناء |
| العمل على فرع لا يُنشر | حسم الفرع — `main` (مجموعة 1) |
| الكارت يبدو بانراً | الالتزام الحرفي بالمواصفات |
| **قاعدة `_redirects` غلط تكسر أو تُتجاهَل بصمت** 🆕 | تحقق إلزامي على Preview + اختبار صفحة حقيقية من كل نوع قبل وبعد Production |

## 10. خارج النطاق

- تشغيل الفيديو داخل الفارس
- أماكن عرض إضافية خارج صفحة الخدمة والرئيسية وصفحتَي المناطق
- تنفيذ `docs/datacodex-cross-link-map.md` — موثّقة لكنها **غير منفّذة في الكود**، وهي عمل منفصل
- **تعديل أي شيء في مشروع داتا كودكس أو أداة النشر — خارج صلاحية هذا المشروع**
- ملاحظة رُصدت أثناء المراجعة ولا تخص هذه الخطة: النص الاحتياطي في `en/index.html:614`
  عربي («تصفح») رغم أن الصفحة إنجليزية، يُرجَّح أن `data-i18n` يعالجه وقت التشغيل —
  يستحق فحصاً في خطة منفصلة، لا يُلمس هنا

## 11. التوثيق بعد التنفيذ

`project-context.md` · `project-key.md` · `changelog.md` + رفع رقم الإصدار.

## 12. أسئلة مفتوحة للمطور

لا توجد فجوات غير محسومة حالياً — كل بند وارد في هذه الخطة مصدره
`docs/reviews/03-review-plan-59-pre-implementation.md` صراحةً. هذا القسم يبقى فارغاً
عمداً حتى يظهر سؤال فعلي أثناء التنفيذ، وقتها يُضاف هنا قبل المتابعة، لا يُحسم من تلقاء
الوكيل.

# انتهى النسخ
