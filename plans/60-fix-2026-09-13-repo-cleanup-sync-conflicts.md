# تنظيف المستودع من ملفات sync-conflict والوصول لنسخة عمل نظيفة (تمهيد لخطة 59)
> الإصدار: 1.0.0
> التاريخ: 2026-09-13
> الحالة: ✅ مكتملة — نُفِّذت 2026-09-13 بعد اعتماد المطور في `docs/reviews/02-review-repo-cleanup-decisions.md` (بقي فقط الدفع لـGitHub بموافقة المطور)

## الهدف

المستودع فيه 100 إدخال غير نظيف في `git status` — أغلبها (68 ملف) نسخ `sync-conflict` مكررة من Syncthing، والباقي (32 إدخال) خليط من تعديلات حقيقية غير مثبتة، مستندات، وملفات سكرِبت مؤقتة. الهدف من الخطة دي:

1. حسم مصير كل ملف من الـ100 بأمان — نحتفظ بأي شغل حقيقي، ونشيل التكرار والقمامة، من غير ما نفقد حاجة.
2. الوصول لنسخة عمل (`working tree`) مطابقة تماماً لآخر commit معتمد ومتزامنة مع GitHub، عشان تبقى نقطة انطلاق نظيفة لخطة 59.
3. حسم فرع النشر (`master` مقابل `main`) بمعرفة المطور، لأن خطة 59 بتفترض "نسخة نظيفة" و"فرع نشر محسوم" كشرطين مسبقين.

## الوضع الحالي (نتائج الفحص — أرقام حقيقية من المستودع)

### 1. الفروع والـ remote

| الفرع | آخر commit | الحالة |
|-------|-----------|--------|
| `master` (محلي، الحالي) | `a3dbf00` | **مطابق تماماً** لـ `origin/main` و `origin/master` (نفس الـ SHA بالضبط) |
| `origin/main` | `a3dbf00` | يشير إليه `origin/HEAD` — هو فرع النشر الفعلي على Cloudflare Pages |
| `origin/master` | `a3dbf00` | مطابق لـ `origin/main` تماماً |
| `origin/prelaunch-2026-05-06` (الفرع اللي `master` المحلي "بيتابعه" حسب `git branch -vv`) | `b162eea` | **متأخر** — ناقصه commit-ين (`087a62a`, `a3dbf00`) شاملين خطة 54 وخطة 57 |
| `origin/backup-main-before-2026-05-06` | `b162eea` | نسخة احتياطية قديمة، نفس نقطة `prelaunch-2026-05-06` |

**الخلاصة المهمة:** المحتوى فعلاً محدّث — `master` المحلي مطابق حرفياً لـ `origin/main` (نفس الـ commit hash، `git diff` فاضي تماماً بينهم). المشكلة مش إننا محتاجين نسحب كود جديد من GitHub، المشكلة إن الفرع المحلي بيتابع (`tracking`) فرع `prelaunch-2026-05-06` القديم بدل `main`، وده بيخلي `git status`/`git push` الافتراضي يحسب المسافة غلط ("ahead by 2"). **الفحص ده تم بفحص الـ refs المحلية المخزنة عندي فقط — لسه محتاج `git fetch` (خطوة أولى في التنفيذ) للتأكد مفيش commits جديدة اتدفعت على GitHub من وقت آخر مزامنة محلية.**

### 2. ملفات sync-conflict (68 ملف)

- **كلها untracked** (`??` في `git status`) — مفيش ولا واحدة منها اتحفظت في تاريخ git من قبل.
- بتتوزع على **34 ملف أصلي**، كل واحد منهم له **نسختين** conflict (تاريخ `20260519` و`20260520`).
- **تحقّقت:** كل زوج (النسخة بتاريخ 19 والنسخة بتاريخ 20 لنفس الملف) **متطابق بايت-لبايت** (`diff -q` رجع فاضي) — يعني الـ68 ملف فعلياً هم 34 محتوى فريد مكرر مرتين، مش 68 نسخة مختلفة.
- **تحقّقت من المحتوى:** كل نسخ الـconflict بتاريخ تعديل (`mtime`) = 6 مايو 2026، يعني هي لقطة قديمة من وقت "pre-launch website updates" (commit `b162eea`)، **قبل** كل التعديلات اللي اتعملت بعد كده والمعتمدة فعلاً في `HEAD` الحالي (خطة 54 يوم 18 مايو، خطة 57 يوم 10 يوليو، تحديثات project-context لحد 23 يوليو).
- **عيّنة مقارنات فعلية:**
  - `index.html` الحالي (working tree) **مطابق 100% لـ `HEAD`** (`git diff --stat HEAD -- index.html` فاضي) — يعني الملف الحالي مش فيه أي تعديل غير محفوظ. مقابل نسخة الـconflict: فرق **2016 سطر** عن `HEAD` (نسخة قديمة قبل خطة 54 و57).
  - `changelog.md`: النسخة الحالية 17,498 بايت (آخر تعديل 10 يوليو) مقابل نسخة الـconflict 12,740 بايت (6 مايو) — فرق حجم كبير يعكس إضافات لاحقة موثقة أصلاً في الملف الحالي.
  - `project-context.md`: نسخة الـconflict (6 مايو، 11,933 بايت) بتوقف عند "الموقع لسه في pre-launch"، بينما النسخة الحالية (29,896 بايت) موثق فيها كل حالة خطة 54، 55، 57، 58 وتحديثات لحد 23 يوليو. مفيش أي معلومة في نسخة الـconflict مش موجودة (بصيغة أحدث) في النسخة الحالية.
  - نفس النمط تكرر في `project-key.md` و`sitemap.xml` وكل صفحات `services/` و`en/services/`.
- **الخلاصة:** الـ68 ملف كلها نسخ قديمة تجاوزتها التعديلات المعتمدة فعلاً، ومفيش فيها أي محتوى فريد غير موجود في النسخة الحالية أو في تاريخ git. **آمن حذفها بعد نسخة احتياطية** (خطوة إلزامية أدناه، مش افتراض بدون تحقق).

### 3. باقي الإدخالات غير النظيفة (32 إدخال) — شغل حقيقي محتمل، مش قمامة

| المجموعة | الملفات | التقييم |
|----------|---------|---------|
| **تعديلات حقيقية غير مثبتة (M)** | `project-context.md`, `project-key.md`, `plans/54-fix-2026-05-18-canonical-clean-url.md` | **شغل حقيقي** — `git diff` بيّن تحديثات موثقة (إغلاق خطة 57، بدء خطة 58 OAuth، إلخ). لازم تتحفظ/تتثبّت، مش تتحذف. |
| **خطط جديدة غير متتبعة** | `plans/48, 49, 52, 53, 55, 58, 59` | ملفات خطط شرعية لسه ما اتعملهاش commit — **شغل حقيقي**. |
| **مستندات جديدة** | `docs/Chats/`, `docs/Google_indexing/`, `docs/SEO_Indexing_and_Publishing_Strategy.md`, `docs/audits/`, `docs/indexing/`, `docs/reviews/`, ملف عربي "أنت خبير Technical SEO..." | مستندات عمل — **شغل حقيقي**، تحتاج فقط قرار: تتضاف لـ git ولا تفضل محلية. |
| **`.claude/`** | `settings.json`, `settings.local.json` | إعدادات أداة العمل — عادة بتتسجل في `.gitignore` مش في التتبع، قرار المطور. |
| **`.gitignore` نفسه** | جديد، بيستثني `/secrets/` بس حالياً | **شغل حقيقي** لازم يتثبّت، وناقصه أنماط تانية (تفصيل تحت). |
| **🔴 حرج — `docs/cloudflare_Token.md`** | يحتوي **Cloudflare Account ID + API Token فعّال بصيغة نص صريح** | **ثغرة أمنية حقيقية** — الملف مش في `.gitignore`، غير متتبع لسه لكنه معرّض لدخول git بالخطأ في أي `git add .` مستقبلي. لازم يتحرك خارج المستودع أو داخل `/secrets/` (المستثنى فعلاً) **قبل أي تعامل مع git إضافة/commit**. |
| **سكربتات وملفات مؤقتة واضحة القِدَم** | `fix_footer.ps1` (سكربت يدوي قديم من 4 مايو، وظيفته اتعملت وانتهت)، `footer_temp.html` (**حجمه صفر بايت** — ملف فاضي تماماً) | تبدو قمامة، لكن القرار للمطور — مذكورة كبند مراجعة، مش حذف تلقائي. |
| **`scratch/`** | 7 ملفات (سكربتات فحص/تدقيق مؤقتة `.py`/`.ps1` وملف اختبار) | تبدو أدوات عمل مؤقتة مساعدة — مش جزء من الموقع، لكن ممكن يكون فيها مرجعية لاحقاً. قرار المطور: تتأرشف محلياً أو تتحذف. |
| **`temp_archive/`** | 16 ملف (قوالب وسكربتات من 3-5 مايو 2026، أقدم من commit الإطلاق `b162eea`) | ملفاتها أقدم حتى من نسخ الـsync-conflict، وبعضها (`phase5-*.js`, `phase6-audit.py`) بتشبه أدوات مرحلة تطوير منتهية. مرشحة للأرشفة الخارجية، بقرار المطور. |

### 4. `.gitignore`

المحتوى الحالي:
```
# Local secrets - never commit (Plan 58 OAuth credentials, refresh tokens)
/secrets/
```

**لا يستثني** أنماط `*.sync-conflict-*` ولا `docs/cloudflare_Token.md` ولا `.claude/settings.local.json` ولا `scratch/`/`temp_archive/`. ده سبب مباشر ليه ملفات الـsync-conflict ظهرت في `git status` كـ untracked بدل ما تتجاهل من الأساس — Syncthing هيستمر يعمل نسخ conflict مستقبلاً طالما الأداتين (git + Syncthing) شغالين على نفس المجلد بدون استثناء.

## المتطلبات

- عدم فقدان أي شغل حقيقي (خطط، مستندات، تعديلات project-context/project-key/plan-54).
- نسخة احتياطية موثقة (فرع git أو أرشيف خارج المستودع) **قبل** أي حذف جماعي.
- كل أمر حذف جماعي (`git clean` أو ما شابه) يُعرض بـ `--dry-run` على المطور قبل التنفيذ الفعلي.
- تأمين `docs/cloudflare_Token.md` فوراً كأولوية أمنية، منفصلة عن باقي التنظيف.
- تحديث `.gitignore` ليمنع تكرار مشكلة sync-conflict مستقبلاً.
- حسم المطور صراحة لفرع النشر ولحساب GitHub قبل أي `fetch`/`pull`/`push`.
- الوصول النهائي: `git status` نظيف (لا شيء غير مفسَّر)، و`working tree` مطابق لـ `origin/main` زائد أي commits جديدة موثقة بوضوح.

## خطوات التنفيذ

### المجموعة 1: تأمين عاجل + نسخة احتياطية (قبل أي حذف)

- [x] [🔴🤖] **تأمين `docs/cloudflare_Token.md` فوراً** — نقل الملف خارج المستودع (أو لداخل `/secrets/` المستثنى فعلاً في `.gitignore`) قبل أي أمر `git add`. هذا يمنع تسرّب الـ API Token لو حصل commit جماعي بالخطأ.
- [x] [👤] **المطور يؤكد:** هل الـ Cloudflare API Token ده لسه صالح ومستخدم؟ لو آه، بعد النقل يُنصح بمراجعة صلاحياته/تدويره كإجراء احترازي عام (خارج نطاق هذه الخطة، للعلم فقط).
- [x] [🤖] إنشاء فرع نسخة احتياطية محلي من الحالة الحالية بالكامل (working tree + الفروع الحالية) — مثلاً `backup/pre-cleanup-2026-09-13` — يشمل تثبيت (commit مؤقت أو `git stash`) لكل التعديلات وغير المتتبعات قبل أي حذف، بحيث أي حاجة تتشال ممكن نرجعها.
- [x] [🤖] بالتوازي، نسخ فعلي (خارج مجلد المستودع، مثلاً على مسار مؤقت) لكل الملفات الـ32 غير-sync-conflict كطبقة أمان إضافية لا تعتمد على git وحده.

⏸️ Review Gate — انتظر تأكيد المطور إن التأمين والنسخة الاحتياطية تمّا قبل الانتقال لحذف أي ملف.

### المجموعة 2: حسم الشغل الحقيقي غير المثبّت

- [x] [👤] **قرار المطور:** التعديلات غير المثبتة في `project-context.md`, `project-key.md`, `plans/54-fix-2026-05-18-canonical-clean-url.md` — تتعمل لها commit منفصل الآن (قبل التنظيف) ولا تُترك للمطور يراجعها يدوياً أولاً؟
- [x] [🤖] تنفيذ القرار: إما commit موثّق للتعديلات الثلاثة، أو تركها كما هي وتأجيلها بوضوح في التقرير النهائي.
- [x] [👤] **قرار المطور:** الخطط الجديدة (`plans/48, 49, 52, 53, 55, 58, 59`) والمستندات الجديدة (`docs/Chats/`, `docs/Google_indexing/`, `docs/audits/`, `docs/indexing/`, `docs/reviews/`, `docs/SEO_Indexing_and_Publishing_Strategy.md`, الملف العربي عن Technical SEO) — تتضاف لـ git الآن (commit توثيقي واحد أو أكثر) ولا تفضل محلية غير متتبعة مؤقتاً؟
- [x] [🤖] تنفيذ القرار حسب رد المطور.
- [x] [👤] **قرار المطور:** `.claude/` (settings.json, settings.local.json) — يُضاف لـ `.gitignore` (الأرجح) ولا يُتتبّع في git؟
- [x] [🤖] تحديث `.gitignore` ليشمل: أنماط `*.sync-conflict-*.*` (لمنع تكرار المشكلة)، `.claude/` أو الملفات الحساسة منه حسب قرار المطور، وأي أنماط تانية اتفق عليها.

⏸️ Review Gate — انتظر اعتماد المطور لقرارات هذه المجموعة قبل الانتقال لحذف ملفات sync-conflict والقمامة المحتملة.

### المجموعة 3: حذف ملفات sync-conflict والقمامة المرشحة (بعد الموافقة الصريحة فقط)

- [x] [🤖] تشغيل حذف ملفات sync-conflict الـ68 عبر **قائمة مسارات صريحة مُولَّدة من `git status --porcelain` نفسه** (وليس أي glob عام زي `*.sync-conflict-*` تنفيذي مباشر) — نفس تحذير الضمان 2 في خطة 59: أي glob فضفاض في هذا المستودع ملوّث بملفات sync-conflict وممكن يلتقط حاجة غير مقصودة.
- [x] [🤖] عرض قائمة الـ68 ملف كاملة على المطور كـ dry-run (نص فقط، بدون تنفيذ) قبل الحذف الفعلي.
- [x] [👤] موافقة صريحة على قائمة الـ68 ملف تحديداً.
- [x] [🤖] تنفيذ الحذف الفعلي لملفات sync-conflict فقط.
- [x] [👤] **قرار المطور لكل بند من بنود "القمامة المحتملة":**
  - `fix_footer.ps1` + `footer_temp.html` (الأخير 0 بايت) — حذف أم أرشفة؟
  - `scratch/` (7 ملفات) — حذف، أرشفة خارجية، أم تُترك كما هي (غير متتبعة، لا تضر)؟
  - `temp_archive/` (16 ملف، أقدم من الإطلاق) — نفس الخيارات؟
- [x] [🤖] تنفيذ قرار المطور لكل بند.

⏸️ Review Gate — انتظر تأكيد المطور إن نتيجة `git status` بقت نظيفة ومطابقة للمتوقع قبل الانتقال للتعامل مع الـ remote.

### المجموعة 4: حسم فرع النشر والتزامن مع GitHub

- [x] [👤] **تأكيد حساب GitHub وبيانات الاعتماد** — قبل أي `fetch`/`pull`/`push`، المطور يؤكد الحساب المستخدم للوصول لمستودع `Alfareslab/alfares-website` وأن بيانات الاعتماد (SSH key / token) جاهزة ومفعّلة على هذه الآلة. **لا افتراض لأي حساب.**
- [x] [🤖] `git fetch --all` (قراءة فقط، لا يغيّر working tree) للتأكد من عدم وجود commits جديدة على أي من الفروع الأربعة منذ آخر فحص محلي.
- [x] [👤] **قرار المطور — حسم فرع النشر:** `origin/HEAD` يشير فعلياً لـ `origin/main`، و`origin/main` = `origin/master` = `master` المحلي بالفعل (نفس commit `a3dbf00`). القرار المطلوب: هل نعتمد `main` رسمياً كفرع العمل والنشر الوحيد، ونوقف تتبّع `master` المحلي لـ `origin/prelaunch-2026-05-06` القديم؟ ولا فيه سبب لاستمرار وجود `master`/`prelaunch-2026-05-06` كفروع نشطة؟
- [x] [🤖] تنفيذ قرار المطور: إعادة ضبط `upstream tracking` للفرع المحلي ليتابع `origin/main` بدل `origin/prelaunch-2026-05-06` (أمر `git branch --set-upstream-to`، لا يغيّر أي ملف، فقط مرجع التتبع) — **يُعرض على المطور قبل التنفيذ**.
- [x] [👤] **قرار المطور:** مصير الفروع القديمة على GitHub (`prelaunch-2026-05-06`, `backup-main-before-2026-05-06`) — تُترك كأرشيف، أم تُحذف من الـ remote؟ (حذف فرع remote عملية لا رجعة فيها بسهولة — تحتاج موافقة صريحة منفصلة ولا تُنفَّذ ضمن هذه الخطة تلقائياً).

⏸️ Review Gate — انتظر اعتماد المطور على قرار فرع النشر قبل أي commit/push فعلي للتعديلات المجمّعة من المجموعة 2.

### المجموعة 5: الإغلاق والتوثيق

- [x] [🤖] `git status` نهائي — يجب أن يكون نظيفاً أو يحتوي فقط على ما اتفق عليه صراحة مع المطور.
- [x] [👤] موافقة نهائية على أن الحالة تمثل "نسخة عمل نظيفة" جاهزة لخطة 59.
- [x] [🤖] تحديث `project-context.md` (قسم الحالة + المشاكل المعروفة) بخلاصة التنظيف ونتيجته.
- [x] [🤖] تحديث `project-key.md` لو فيه ملفات جديدة اتضافت فعلياً للتتبع.
- [x] [🤖] تسجيل التغيير في `changelog.md`.

⏸️ Review Gate — نهاية الخطة، بانتظار اعتماد المطور للانتقال لتنفيذ خطة 59.

## الملفات المتأثرة

| الملف | نوع التغيير |
|-------|-------------|
| 68 ملف `*.sync-conflict-*.*` (34 أصل × 2) | حذف (بعد نسخة احتياطية وموافقة صريحة) |
| `docs/cloudflare_Token.md` | نقل خارج المستودع أو لداخل `/secrets/` — عاجل |
| `project-context.md`, `project-key.md`, `plans/54-fix-2026-05-18-canonical-clean-url.md` | commit للتعديلات الحالية (بقرار المطور) |
| `plans/48, 49, 52, 53, 55, 58, 59` | إضافة للتتبع (بقرار المطور) |
| `docs/Chats/`, `docs/Google_indexing/`, `docs/audits/`, `docs/indexing/`, `docs/reviews/`, `docs/SEO_Indexing_and_Publishing_Strategy.md`, ملف Technical SEO العربي | إضافة للتتبع (بقرار المطور) |
| `.gitignore` | تعديل — إضافة أنماط `sync-conflict` و`.claude/` وغيرها |
| `.claude/` | استثناء من التتبع (الأرجح، بقرار المطور) |
| `fix_footer.ps1`, `footer_temp.html`, `scratch/*`, `temp_archive/*` | حذف أو أرشفة خارجية (بقرار المطور) |
| فرع التتبع المحلي (`master` → `origin/main`) | إعادة ضبط مرجع تتبع فقط، لا تعديل ملفات |

## المخاطر والتخفيف

| الخطر | التخفيف |
|-------|---------|
| حذف نسخة sync-conflict فيها فعلاً تعديل لم يُحفظ في مكان تاني | تحقّقنا فعلياً إن كل نسخ الـconflict أقدم من `HEAD` الحالي بفروق مئات-آلاف الأسطر، ومطابقة لحالة 6 مايو المتجاوَزة — بالإضافة لنسخة احتياطية كاملة (فرع + أرشيف خارجي) قبل أي حذف |
| glob عام يلتقط ملفات غير مقصودة أثناء الحذف | استخدام قائمة مسارات صريحة فقط (نفس قيد خطة 59)، وعرض dry-run نصي قبل التنفيذ |
| تسرّب Cloudflare API Token في commit مستقبلي | نقله فوراً في المجموعة 1 قبل أي `git add`، ويُنصح بتدوير الـ Token لاحقاً كإجراء احترازي |
| الخلط بين "تحديث من GitHub" و"الفرع أصلاً محدّث" يؤدي لعمل `pull`/`merge` غير ضروري وربما يكسر شيئاً | التقرير يوضح صراحة إن `master` = `origin/main` بالفعل (نفس commit) — لا حاجة لـ merge، فقط تصحيح مرجع التتبع |
| حذف فرع remote قديم يفقد تاريخاً مفيداً لاحقاً | حذف الفروع البعيدة **غير مُدرج ضمن التنفيذ التلقائي** لهذه الخطة — بند قرار منفصل فقط |
| فقدان سكربتات `scratch/`/`temp_archive/` قد تكون مرجعية لعمل مستقبلي مشابه | لا حذف تلقائي — بند قرار صريح للمطور، مع اقتراح أرشفة خارج المستودع بدل الحذف النهائي |

## مسار الرجوع (Rollback)

1. الفرع الاحتياطي `backup/pre-cleanup-2026-09-13` (من المجموعة 1) يحتفظ بكامل حالة المستودع (متتبَّع وغير متتبَّع عبر commit/stash) قبل أي حذف — أي ملف اتشال يمكن استرجاعه منه بـ `git checkout backup/pre-cleanup-2026-09-13 -- <path>`.
2. النسخة الخارجية (خارج مجلد المستودع، من المجموعة 1) تغطي نفس الملفات كطبقة أمان مستقلة عن git، للرجوع لو الفرع الاحتياطي نفسه اتأثر بطريق الخطأ.
3. لو أي خطوة في المجموعة 4 (تعديل مرجع التتبع) سببت التباس، الحل فوري وآمن: `git branch --set-upstream-to=origin/prelaunch-2026-05-06 master` يرجّع الوضع القديم — العملية مجرد مرجع، لا تلمس أي كود أو commit.
4. لا يوجد أي `push` أو حذف فرع remote في هذه الخطة، فمفيش خطوة هنا غير قابلة للرجوع على مستوى GitHub نفسه.

## ملاحظات

- الفحص في هذه الخطة اعتمد على الـ refs المحلية المخزّنة وقت الفحص (بدون `git fetch`) لأن الصلاحية المتاحة وقت كتابة الخطة قراءة فقط. **أول خطوة تنفيذية فعلية يجب أن تكون `git fetch --all`** للتأكد من عدم وجود تغييرات جديدة على GitHub منذ ذلك الوقت.
- الاكتشاف الأمني (`docs/cloudflare_Token.md`) تم إبلاغه فوراً في هذا التقرير كما ينص بروتوكول المراجعة (`02-audit-rules.md` § 5) — لا ينتظر اعتماد باقي الخطة.
- خطة 59 (`plans/59-datacodex-cards-bridge.md`) تفترض صراحة "نسخة نظيفة" و"فرع نشر محسوم" كشرطين قبل بدء المجموعة 1 فيها — هذه الخطة (60) هي السابقة المباشرة لها، ولا ينبغي بدء تنفيذ خطة 59 قبل إغلاق المجموعة 5 هنا بموافقة المطور.


---

## سجل التنفيذ الفعلي — 2026-09-13

| البند | ما حدث فعلاً |
|------|-------------|
| `git fetch --all` | لا commits جديدة على GitHub. `HEAD` المحلي = `origin/main` = `a3dbf00` |
| النسخة الاحتياطية | أرشيف خارجي 119 ملف / 4 MB في `F:/Myprojects/Alfareslab_backup_2026-09-13/` |
| فرع احتياطي في git | **لم يُنفَّذ عمداً** — كان سيتطلب commit لملف التوكن نفسه، وهو ما تتفاداه الخطة. الأرشيف الخارجي + تاريخ git يغطيان الرجوع |
| `docs/cloudflare_Token.md` | **حُذف** بقرار المطور (لا نقل). تحقُّق: `git log --all` عليه فاضٍ — لم يدخل أي commit، فلا تسريب |
| 68 ملف sync-conflict | حُذفت بقائمة مسارات صريحة. تحقُّق مسبق: 64 أصلاً مطابق بايت-لبايت لـ`a3dbf00`، و4 أصول (`project-context.md`, `project-key.md`) نسختها المحلية أحدث وأُدخلت في commit |
| **مفاجأة عن الخطة** | `scratch/` و`temp_archive/` **لم تكونا غير متتبعتين** كما افترضت الخطة — 16 ملفاً منهما كان متتبعاً في git. الحذف صار تغييراً حقيقياً في الريبو، وأُرشفت الـ16 من `a3dbf00` قبل الحذف |
| أثر جانبي على الموقع | 8 قوالب HTML يتيمة كانت قابلة للزحف تحت `/temp_archive/` اختفت. لا إشارة لها في `sitemap.xml` ولا في أي صفحة — الأثر إيجابي على SEO |
| `.gitignore` | يمنع الآن `*.sync-conflict-*` و`.claude/` و`scratch/` و`temp_archive/` وملفات الاعتماد |
| الفرع | `master` → أعيدت تسميته `main`، ويتابع `origin/main` بدل `origin/prelaunch-2026-05-06` |
| commits | `0d9ce6c` تنظيف · `f748ed6` توثيق وخطط · `ee67012` سجل الخطة |
| `git status` | **نظيف — صفر إدخال** (كان 101) |
| الدفع لـGitHub | ⏸️ **موقوف بانتظار موافقة المطور** — يُطلق نشر Cloudflare Production |
| حذف `origin/prelaunch-2026-05-06` | ⏸️ **موقوف** — عملية لا رجعة فيها، تحتاج موافقة منفصلة |
